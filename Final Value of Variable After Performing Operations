class Solution:
    def finalValueAfterOperations(self, operations: list[str]) -> int:
        x = 0
        for op in operations:
            if '+' in op:  # Check for increment operation
                x += 1
            else:          # Otherwise, it's a decrement operation
                x -= 1
        return x
