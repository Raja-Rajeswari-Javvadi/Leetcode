class Solution(object):
    def makeTheIntegerZero(self, num1, num2):
        for k in range(1, 65):
            x = num1 - k * num2

            if x < k:
                continue

            set_bits = bin(x).count('1')

            if set_bits <= k:
                return k

        return -1
